<script type=\"text/babel\">\n\n/* ── DATA (identique au player) ───────────────────────────────────── */\nconst POSITION = {\n  states: [{ k:'debout', l:'Debout' },{ k:'accroupi', l:'Accroupi' },{ k:'couche', l:'Couché' }],\n  cost: { debout:{accroupi:-3,couche:-5}, accroupi:{debout:-3,couche:-5}, couche:{debout:-10,accroupi:-10} }\n};\nconst ARM = {\n  states: [{ k:'rangee', l:'Rangée' },{ k:'main', l:\"Main sur l'arme\" },{ k:'clair', l:'Au clair' }],\n  cost: { rangee:{main:-3,clair:-5}, main:{rangee:-5,clair:-3}, clair:{rangee:-10,main:-3} }\n};\nconst FIRE_MODE = {\n  states: [{ k:'cc', l:'Coup par coup' },{ k:'rc', l:'Rafale courte' },{ k:'rl', l:'Rafale longue' }],\n  cost: { cc:{rc:-3,rl:-3}, rc:{cc:-3,rl:-3}, rl:{cc:-3,rc:-3} }\n};\nconst COVER = {\n  states: [\n    { k:'decouvert', l:'Découvert', cost:0 },\n    { k:'partielle', l:'Partielle (50%)', cost:-3 },\n    { k:'importante',l:'Importante (75%)',cost:-5 },\n  ]\n};\nconst VITESSE = {\n  states: [\n    { k:'retardee', l:'Retardée', cost:0, special:'Spécial' },\n    { k:'normale',  l:'Normale',  cost:0 },\n    { k:'precipitee',l:'Précipitée', cost:+3 },\n  ]\n};\n\nconst MAP_ACTIONS = [\n  { k:'move',     l:'Déplacement', icon:'↦' },\n  { k:'attack',   l:'Assaut (tir)', icon:'⚔' },\n  { k:'multi',    l:'Attaque multiple', icon:'⚔⚔', ini:-5 },\n  { k:'melee',    l:'Corps à corps', ini:-3, icon:(\n    <svg viewBox=\"0 0 12 12\" width=\"11\" height=\"11\" fill=\"none\" stroke=\"currentColor\" strokeWidth=\"1.6\" style={{display:'inline-block',verticalAlign:'middle'}}>\n      <path d=\"M2 3.5 L5 6 L2 8.5\"/><path d=\"M10 3.5 L7 6 L10 8.5\"/><path d=\"M5.5 6 L6.5 6\"/>\n    <\u002Fsvg>\n  )},\n  { k:'interact', l:'Interagir', icon:'⚙' },\n];\n\nfunction stateTransitionCost(def, from, to) {\n  if (from === to) return 0;\n  if (def.cost && def.cost[from]) return def.cost[from][to] ?? 0;\n  const t = def.states.find(s => s.k === to);\n  return t?.cost ?? 0;\n}\nfunction nextStateKey(def, cur) {\n  const idx = def.states.findIndex(s => s.k === cur);\n  return def.states[(idx + 1) % def.states.length].k;\n}\n\n/* ─────────────────────────────────────────────────────────────────────\n   PNJs — données démo\n   ───────────────────────────────────────────────────────────────────── */\nconst INITIAL_NPCS = [\n  { id:'cap', name:'Capitaine de la garde', init:11, kind:'boss', avatar:'C',\n    initial:{ position:'debout', arm:'clair', fire:'cc', cover:'partielle', vitesse:'normale' },\n    state:{   position:'debout', arm:'clair', fire:'cc', cover:'partielle', vitesse:'normale' },\n    action:null, status:'todo' },\n  { id:'karr', name:'Karrok', init:7, kind:'enemy', avatar:'K',\n    initial:{ position:'debout', arm:'main', fire:'cc', cover:'decouvert', vitesse:'normale' },\n    state:{   position:'accroupi', arm:'clair', fire:'cc', cover:'partielle', vitesse:'normale' },\n    action:'attack', status:'done', iniDelta:-9 },\n  { id:'tehk', name:'Tehk', init:9, kind:'enemy', avatar:'T',\n    initial:{ position:'debout', arm:'rangee', fire:'cc', cover:'decouvert', vitesse:'normale' },\n    state:{   position:'debout', arm:'main',   fire:'cc', cover:'decouvert', vitesse:'normale' },\n    action:'interact', status:'inprogress', iniDelta:-3 },\n  { id:'sol1', name:'Soldat α', init:7, kind:'minion', avatar:'α',\n    initial:{ position:'debout', arm:'clair', fire:'cc', cover:'decouvert', vitesse:'normale' },\n    state:{   position:'debout', arm:'clair', fire:'cc', cover:'decouvert', vitesse:'normale' },\n    action:null, status:'todo' },\n  { id:'sol2', name:'Soldat β', init:7, kind:'minion', avatar:'β',\n    initial:{ position:'debout', arm:'clair', fire:'cc', cover:'decouvert', vitesse:'normale' },\n    state:{   position:'debout', arm:'clair', fire:'cc', cover:'decouvert', vitesse:'normale' },\n    action:null, status:'todo' },\n  { id:'sol3', name:'Soldat γ', init:7, kind:'minion', avatar:'γ',\n    initial:{ position:'debout', arm:'clair', fire:'cc', cover:'decouvert', vitesse:'normale' },\n    state:{   position:'debout', arm:'clair', fire:'cc', cover:'decouvert', vitesse:'normale' },\n    action:null, status:'todo' },\n  { id:'sol4', name:'Soldat δ', init:7, kind:'minion', avatar:'δ',\n    initial:{ position:'debout', arm:'clair', fire:'cc', cover:'decouvert', vitesse:'normale' },\n    state:{   position:'debout', arm:'clair', fire:'cc', cover:'decouvert', vitesse:'normale' },\n    action:null, status:'todo' },\n];\n\nconst KIND_COLORS = {\n  boss:   { bg:'#2a1a08', border:'#aa6030', text:'#e8b070' },\n  enemy:  { bg:'#1a0a08', border:'#aa3030', text:'#e89090' },\n  minion: { bg:'#1a0a0e', border:'#702030', text:'#aa5070' },\n};\n\nconst STATUS_LABEL = {\n  todo:       { l:'À déclarer', color:'#456575', glyph:'○' },\n  inprogress: { l:'En cours',   color:'#aa8a30', glyph:'◷' },\n  done:       { l:'Déclaré',    color:'#5a7080', glyph:'✓' },\n};\n\n/* ── Inline chip selector ─────────────────────────────────────────── */\nfunction InlineChip({ label, def, initial, current, onChange, accent='#3a8aaa', mixed=false }) {\n  const cur  = def.states.find(s => s.k === current);\n  const next = nextStateKey(def, current);\n  const isInitial = current === initial;\n  const cost = stateTransitionCost(def, initial, current);\n  return (\n    <div onClick={() => onChange(next)} title={`Clic pour : ${def.states.find(s=>s.k===next)?.l}`} style={{\n      display:'inline-flex', alignItems:'center', gap:5,\n      padding:'3px 7px',\n      background: mixed ? '#181410' : (isInitial ? '#0a1018' : '#162028'),\n      border:`1px solid ${mixed ? '#aa8a30' : (isInitial ? '#15212e' : accent+'66')}`,\n      cursor:'pointer', borderRadius:2\n    }}>\n      <span className=\"mono\" style={{fontSize:7, color:'#456575', letterSpacing:'0.1em'}}>{label}<\u002Fspan>\n      <span className=\"ui\" style={{fontSize:9, color: mixed ? '#aa8a30' : '#dde7ee', fontWeight:600}}>\n        {mixed ? '— mixte —' : cur?.l}\n      <\u002Fspan>\n      {!mixed && !isInitial && cost !== 0 && (\n        <span className=\"mono\" style={{fontSize:7, color: cost > 0 ? '#3aaa6a' : '#aa5030'}}>{cost > 0 ? '+' : ''}{cost}<\u002Fspan>\n      )}\n    <\u002Fdiv>\n  );\n}\n\n/* ─────────────────────────────────────────────────────────────────────\n   COMBINED PANEL — Roster + Action stack\n   ───────────────────────────────────────────────────────────────────── */\nfunction CombinedPanel({ npcs, activeId, setActiveId, selectedIds, toggleSelect, selectAll, selectNone, onUpdateState, onDeclareAction, onValidate, onUndo }) {\n  const total   = npcs.length;\n  const done    = npcs.filter(n => n.status === 'done').length;\n  const sel     = selectedIds.size;\n  const batchMode = sel >= 2;\n  const targetIds = batchMode ? [...selectedIds] : (activeId ? [activeId] : []);\n  const targets   = npcs.filter(n => targetIds.includes(n.id));\n\n  function aggregate(field) {\n    if (!targets.length) return null;\n    const vals = new Set(targets.map(t => t.state[field]));\n    return vals.size === 1 ? [...vals][0] : '__mixed__';\n  }\n  function aggregateInitial(field) {\n    if (!targets.length) return null;\n    const vals = new Set(targets.map(t => t.initial[field]));\n    return vals.size === 1 ? [...vals][0] : '__mixed__';\n  }\n  function update(field, value) {\n    targetIds.forEach(id => onUpdateState(id, field, value));\n  }\n\n  const cur = {\n    position: aggregate('position'), arm: aggregate('arm'),\n    fire: aggregate('fire'), cover: aggregate('cover'), vitesse: aggregate('vitesse'),\n  };\n  const init = {\n    position: aggregateInitial('position'), arm: aggregateInitial('arm'),\n    fire: aggregateInitial('fire'), cover: aggregateInitial('cover'), vitesse: aggregateInitial('vitesse'),\n  };\n\n  const actions = new Set(targets.map(t => t.action));\n  const currentAction = actions.size === 1 ? [...actions][0] : null;\n\n  const iniDeltas = targets.map(t => t.iniDelta ?? 0);\n  const sameIni   = iniDeltas.every(d => d === iniDeltas[0]);\n  const allDone   = targets.length > 0 && targets.every(t => t.status === 'done');\n\n  return (\n    <div style={{\n      width:440, flexShrink:0,\n      background:'rgba(8,12,20,0.97)',\n      borderLeft:'1.5px solid #15212e',\n      display:'flex', flexDirection:'column'\n    }}>\n\n      {/* HEADER — active PNJ / batch context */}\n      <div style={{\n        padding:'10px 14px',\n        background: batchMode ? '#2a1810' : '#06080e',\n        borderBottom:'1px solid #15212e'\n      }}>\n        <div style={{display:'flex', alignItems:'center', gap:8, marginBottom:2}}>\n          <span className=\"mono\" style={{\n            fontSize:9, letterSpacing:'0.15em', fontWeight:600,\n            color: batchMode ? '#e8c870' : '#3a8aaa'\n          }}>\n            {batchMode ? `BATCH — ${targets.length} PNJs` : 'PHASE 1 — DÉCLARATION'}\n          <\u002Fspan>\n          <div style={{flex:1}}/>\n          <span className=\"mono\" style={{fontSize:9, color:'#7a8a99'}}>{done}/{total} déclarés<\u002Fspan>\n        <\u002Fdiv>\n        <div className=\"cav\" style={{fontSize:12, color: batchMode ? '#e8b070' : '#aaccdd'}}>\n          {batchMode\n            ? `${targets.map(t => t.name).join(', ')}`\n            : (targets[0]?.name ?? '— aucun PNJ sélectionné —')\n          }\n        <\u002Fdiv>\n      <\u002Fdiv>\n\n      {/* TACTIQUE */}\n      <div style={{padding:'8px 14px', borderBottom:'1px solid #15212e'}}>\n        <div style={{marginBottom:5}}>\n          <span className=\"mono\" style={{fontSize:8, color:'#5a8aaa', letterSpacing:'0.12em', fontWeight:600}}>TACTIQUE<\u002Fspan>\n        <\u002Fdiv>\n        <div style={{display:'flex', gap:5, flexWrap:'wrap'}}>\n          <InlineChip label=\"POSTURE\" def={POSITION} initial={init.position} current={cur.position}\n            onChange={(v)=>update('position',v)} mixed={cur.position==='__mixed__'}/>\n          <InlineChip label=\"COUVERT\" def={COVER}    initial={init.cover}    current={cur.cover}\n            onChange={(v)=>update('cover',v)} mixed={cur.cover==='__mixed__'}/>\n          <InlineChip label=\"VITESSE\" def={VITESSE}  initial={init.vitesse}  current={cur.vitesse}\n            onChange={(v)=>update('vitesse',v)} accent=\"#aa6a30\" mixed={cur.vitesse==='__mixed__'}/>\n        <\u002Fdiv>\n      <\u002Fdiv>\n\n      {/* ARMEMENT */}\n      <div style={{padding:'8px 14px', borderBottom:'1px solid #15212e'}}>\n        <div style={{marginBottom:5}}>\n          <span className=\"mono\" style={{fontSize:8, color:'#aa6a30', letterSpacing:'0.12em', fontWeight:600}}>ARMEMENT<\u002Fspan>\n        <\u002Fdiv>\n        <div style={{display:'flex', gap:5, flexWrap:'wrap'}}>\n          <InlineChip label=\"ARME\" def={ARM}       initial={init.arm}  current={cur.arm}\n            onChange={(v)=>update('arm',v)} mixed={cur.arm==='__mixed__'}/>\n          <InlineChip label=\"TIR\"  def={FIRE_MODE} initial={init.fire} current={cur.fire}\n            onChange={(v)=>update('fire',v)} mixed={cur.fire==='__mixed__'}/>\n        <\u002Fdiv>\n      <\u002Fdiv>\n\n      {/* ACTION */}\n      <div style={{padding:'10px 14px', borderBottom:'1px solid #15212e'}}>\n        <div style={{marginBottom:6}}>\n          <span className=\"mono\" style={{fontSize:8, color:'#aa8a30', letterSpacing:'0.12em', fontWeight:600}}>ACTION<\u002Fspan>\n        <\u002Fdiv>\n        <div style={{display:'grid', gridTemplateColumns:'1fr 1fr', gap:5}}>\n          {MAP_ACTIONS.map(a => {\n            const active = currentAction === a.k;\n            return (\n              <div key={a.k} onClick={()=>targetIds.forEach(id => onDeclareAction(id, a.k))} style={{\n                padding:'6px 8px',\n                background: active ? '#2a1e10' : '#0a1018',\n                border:`1px solid ${active ? '#aa8a30' : '#15212e'}`,\n                cursor:'pointer'\n              }}>\n                <div style={{display:'flex', alignItems:'center', gap:5}}>\n                  <span style={{fontSize:11, color: active ? '#e8c870' : '#7a8a99'}}>{a.icon}<\u002Fspan>\n                  <span className=\"ui\" style={{fontSize:10, color: active ? '#e8c870' : '#aaccdd', flex:1, fontWeight: active?600:400}}>{a.l}<\u002Fspan>\n                  {a.ini && <span className=\"mono\" style={{fontSize:8, color:'#aa5030'}}>{a.ini}<\u002Fspan>}\n                <\u002Fdiv>\n              <\u002Fdiv>\n            );\n          })}\n        <\u002Fdiv>\n      <\u002Fdiv>\n\n      {/* ROSTER — intégré dans le panel, occupe l'espace restant */}\n      <div style={{\n        flex:1, minHeight:0,\n        display:'flex', flexDirection:'column',\n        background:'#070a10'\n      }}>\n        {/* Roster header — progress + batch toggle */}\n        <div style={{\n          padding:'6px 14px',\n          borderBottom:'1px solid #15212e',\n          background:'#060810',\n          display:'flex', alignItems:'center', gap:6\n        }}>\n          <span className=\"mono\" style={{fontSize:8, color:'#aa6030', letterSpacing:'0.12em', fontWeight:600, flex:1}}>\n            ROSTER — {total} PNJs\n          <\u002Fspan>\n          {sel > 0 && (\n            <span onClick={selectNone} style={{\n              fontSize:8, color:'#aa8a30', cursor:'pointer',\n              fontFamily:'Share Tech Mono', letterSpacing:'0.08em'\n            }}>✕ déselect. ({sel})<\u002Fspan>\n          )}\n          {sel < total && (\n            <span onClick={selectAll} style={{\n              fontSize:8, color:'#456575', cursor:'pointer',\n              fontFamily:'Share Tech Mono', letterSpacing:'0.08em'\n            }}>tout<\u002Fspan>\n          )}\n        <\u002Fdiv>\n\n        {/* List */}\n        <div style={{flex:1, overflow:'auto'}}>\n          {npcs.map(n => {\n            const isActive = !batchMode && n.id === activeId;\n            const isSel    = selectedIds.has(n.id);\n            const kind     = KIND_COLORS[n.kind];\n            const status   = STATUS_LABEL[n.status];\n            return (\n              <div key={n.id} onClick={() => setActiveId(n.id)} style={{\n                display:'flex', alignItems:'center', gap:6,\n                padding:'5px 12px 5px 8px',\n                background: isActive ? '#0a1828' : (isSel ? '#181410' : 'transparent'),\n                borderLeft: isActive ? `3px solid #3a8aaa` : (isSel ? '3px solid #aa8a30' : '3px solid transparent'),\n                borderBottom:'1px solid #0e1520',\n                cursor:'pointer',\n                opacity: n.status === 'done' ? 0.5 : 1,\n                transition:'opacity 0.15s'\n              }}>\n                <input type=\"checkbox\"\n                  checked={isSel}\n                  onClick={(e) => e.stopPropagation()}\n                  onChange={() => toggleSelect(n.id)}\n                  style={{accentColor:'#aa6030', cursor:'pointer'}}/>\n                <span className=\"mono\" style={{fontSize:11, color:status.color, width:10, textAlign:'center', flexShrink:0}}>{status.glyph}<\u002Fspan>\n                <div style={{\n                  width:20, height:20, borderRadius:'50%',\n                  background: kind.bg, border: `1px solid ${kind.border}`,\n                  display:'flex', alignItems:'center', justifyContent:'center', flexShrink:0\n                }}>\n                  <span className=\"mono\" style={{fontSize:9, color: kind.text, fontWeight:600}}>{n.avatar}<\u002Fspan>\n                <\u002Fdiv>\n                <div style={{flex:1, minWidth:0, display:'flex', alignItems:'baseline', gap:5}}>\n                  <span className=\"ui\" style={{\n                    fontSize:10, color: isActive ? '#dde7ee' : '#aaccdd',\n                    fontWeight: isActive ? 600 : 400,\n                    overflow:'hidden', textOverflow:'ellipsis', whiteSpace:'nowrap', flex:1\n                  }}>{n.name}<\u002Fspan>\n                  <span className=\"mono\" style={{fontSize:8, color:'#456575', flexShrink:0}}>INI {n.init}<\u002Fspan>\n                  {n.iniDelta !== undefined && (\n                    <span className=\"mono\" style={{fontSize:8, color: n.iniDelta<0?'#aa5030':'#3aaa6a', flexShrink:0}}>\n                      →{n.init + n.iniDelta}\n                    <\u002Fspan>\n                  )}\n                <\u002Fdiv>\n              <\u002Fdiv>\n            );\n          })}\n        <\u002Fdiv>\n      <\u002Fdiv>\n\n      {/* FOOTER */}\n      <div style={{padding:'8px 14px', background:'#06080e', borderTop:'1.5px solid #15212e'}}>\n        {!batchMode && sameIni && iniDeltas[0] !== 0 && (\n          <div style={{display:'flex', alignItems:'center', gap:8, marginBottom:6}}>\n            <span className=\"mono\" style={{fontSize:9, color:'#456575', letterSpacing:'0.12em', flex:1}}>INI TOTAL<\u002Fspan>\n            <span className=\"mono\" style={{\n              fontSize:18, fontWeight:'bold',\n              color: iniDeltas[0]<-10 ? '#c83030' : (iniDeltas[0]<0 ? '#c86030' : '#3aaa6a')\n            }}>{iniDeltas[0]}<\u002Fspan>\n          <\u002Fdiv>\n        )}\n        {batchMode && (\n          <div style={{\n            padding:'5px 8px', background:'#1a1410', border:'1px solid #aa8a30',\n            marginBottom:6, fontSize:8.5, color:'#aa8a30', fontFamily:'Share Tech Mono', lineHeight:1.35\n          }}>\n            ⚠ coûts INI calculés individuellement (états initiaux différents)\n          <\u002Fdiv>\n        )}\n        <div style={{display:'flex', gap:6}}>\n          <div\n            onClick={() => {\n              if (targetIds.length === 0) return;\n              if (allDone) onUndo(targetIds);\n              else if (currentAction) onValidate(targetIds);\n            }}\n            style={{\n              flex:1,\n              padding:'7px 10px', textAlign:'center',\n              background: allDone ? '#3a0a18' : (currentAction ? '#0a3050' : '#1a1a1a'),\n              border:`1px solid ${allDone ? '#aa3060' : (currentAction ? '#3a8aaa' : '#15212e')}`,\n              cursor: (currentAction || allDone) ? 'pointer' : 'not-allowed',\n              opacity: (currentAction || allDone) ? 1 : 0.5,\n              transition:'all 0.15s'\n            }}>\n            <span className=\"mono\" style={{\n              fontSize:10,\n              color: allDone ? '#e890b0' : '#aaccdd',\n              fontWeight:600, letterSpacing:'0.08em'\n            }}>\n              {allDone\n                ? (batchMode ? `ANNULER ×${targets.length}` : 'ANNULER LA DÉCLARATION')\n                : (batchMode ? `DÉCLARER ×${targets.length}` : 'DÉCLARER')}\n            <\u002Fspan>\n          <\u002Fdiv>\n          <div style={{\n            padding:'7px 12px', textAlign:'center',\n            background: done === total ? '#08322a' : '#1a1a1a',\n            border:`1px solid ${done === total ? '#3aaa6a' : '#15212e'}`,\n            cursor: done === total ? 'pointer' : 'not-allowed',\n            opacity: done === total ? 1 : 0.45\n          }}>\n            <span className=\"mono\" style={{fontSize:10, color: done === total ? '#90ddb0' : '#aaccdd', fontWeight:600, letterSpacing:'0.08em'}}>\n              PHASE 2 →\n            <\u002Fspan>\n          <\u002Fdiv>\n        <\u002Fdiv>\n      <\u002Fdiv>\n    <\u002Fdiv>\n  );\n}\n\n/* ── Composition ──────────────────────────────────────────────────── */\nfunction GMPanel() {\n  const [npcs, setNpcs]               = React.useState(INITIAL_NPCS);\n  const [activeId, setActiveId]       = React.useState('cap');\n  const [selectedIds, setSelectedIds] = React.useState(new Set(['sol1','sol2','sol3']));\n\n  function toggleSelect(id) {\n    setSelectedIds(s => {\n      const n = new Set(s);\n      if (n.has(id)) n.delete(id); else n.add(id);\n      return n;\n    });\n  }\n  function selectAll()  { setSelectedIds(new Set(npcs.map(n => n.id))); }\n  function selectNone() { setSelectedIds(new Set()); }\n\n  function updateState(id, field, value) {\n    setNpcs(ns => ns.map(n => n.id === id ? { ...n, state: { ...n.state, [field]: value } } : n));\n  }\n  function declareAction(id, actionKey) {\n    setNpcs(ns => ns.map(n => n.id === id ? { ...n, action: actionKey } : n));\n  }\n\n  function validateDeclaration(ids) {\n    setNpcs(ns => ns.map(n => ids.includes(n.id) ? { ...n, status: 'done' } : n));\n    // Auto-focus next 'todo' PNJ\n    const startIdx = npcs.findIndex(n => n.id === ids[0]);\n    const ordered  = [...npcs.slice(startIdx+1), ...npcs.slice(0, startIdx)];\n    const candidate = ordered.find(n => n.status === 'todo' && !ids.includes(n.id));\n    if (candidate) setActiveId(candidate.id);\n    setSelectedIds(new Set());\n  }\n\n  function undoDeclaration(ids) {\n    setNpcs(ns => ns.map(n => ids.includes(n.id) ? { ...n, status: 'todo' } : n));\n  }\n\n  return (\n    <div style={{\n      width:'100%', height:'100%', background:'#07080c',\n      display:'flex', fontFamily:'Inter, system-ui'\n    }}>\n      {/* Table 3D — placeholder pleine surface */}\n      <div style={{flex:1, position:'relative', overflow:'hidden', background:'linear-gradient(180deg, #07111e 0%, #050810 100%)'}}>\n        <svg style={{position:'absolute', inset:0, width:'100%', height:'100%', opacity:0.3}}\n          viewBox=\"0 0 800 600\" preserveAspectRatio=\"xMidYMid slice\">\n          <g transform=\"matrix(1.6 0 0 0.7 -100 100)\">\n            {Array.from({length:20}, (_,i) => (\n              <line key={'h'+i} x1=\"0\" y1={i*30} x2=\"600\" y2={i*30} stroke=\"#1a3a5a\" strokeWidth=\"0.5\"/>\n            ))}\n            {Array.from({length:20}, (_,i) => (\n              <line key={'v'+i} x1={i*30} y1=\"0\" x2={i*30} y2=\"600\" stroke=\"#1a3a5a\" strokeWidth=\"0.5\"/>\n            ))}\n          <\u002Fg>\n        <\u002Fsvg>\n        <div style={{position:'absolute', top:14, left:14, fontFamily:'Caveat', fontSize:14, color:'#3a6a8a', maxWidth:380, lineHeight:1.5}}>\n          Le Roster est <strong style={{color:'#e8c870'}}>intégré dans le panel<\u002Fstrong>, juste sous les contrôles.\n          Le focus (clic sur ligne) bascule l'action panel au-dessus.\n          La case à cocher additionne au mode <strong style={{color:'#e8c870'}}>BATCH<\u002Fstrong> — header devient orange, les états deviennent « mixte » si valeurs différentes.\n        <\u002Fdiv>\n      <\u002Fdiv>\n\n      <CombinedPanel\n        npcs={npcs}\n        activeId={activeId}\n        setActiveId={setActiveId}\n        selectedIds={selectedIds}\n        toggleSelect={toggleSelect}\n        selectAll={selectAll}\n        selectNone={selectNone}\n        onUpdateState={updateState}\n        onDeclareAction={declareAction}\n        onValidate={validateDeclaration}\n        onUndo={undoDeclaration}\n      />\n    <\u002Fdiv>\n  );\n}\n\n/* ── App ─────────────────────────────────────────────────────────────── */\nfunction App() {\n  return (\n    <DesignCanvas title=\"VTT Enclume — Panel d'action GM (Roster intégré)\">\n      <DCSection id=\"s1\" title=\"Phase 1 — Déclaration GM (Roster glissé dans le panel)\">\n        <DCArtboard id=\"a1\" label=\"01 — Panel unifié : contrôles + Roster + Footer\" width={1280} height={780}>\n          <GMPanel/>\n        <\u002FDCArtboard>\n      <\u002FDCSection>\n    <\u002FDesignCanvas>\n  );\n}\n\nReactDOM.createRoot(document.getElementById('root')).render(<App />);\n<\u002Fscript>\n<div id=\"root\"><\u002Fdiv>\n\n\n<\u002Fbody><\u002Fhtml>"
  