info:
  name: Create Campaign
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:3001/api/campaigns
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
