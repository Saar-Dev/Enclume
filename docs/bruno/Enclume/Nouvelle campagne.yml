info:
  name: Nouvelle campagne
  type: http
  seq: 11

http:
  method: POST
  url: http://localhost:3001/api/campaigns/
  body:
    type: json
    data: '{ "name": "Test Phase 2" }'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
