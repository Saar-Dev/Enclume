info:
  name: Player
  type: http
  seq: 8

http:
  method: POST
  url: http://localhost:3001/api/auth/register
  body:
    type: json
    data: |-
      {
        "email": "joueur@enclume.com",
        "password": "motdepasse123",
        "username": "Joueur1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
