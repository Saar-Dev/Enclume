info:
  name: Player
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:3001/api/auth/register
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
