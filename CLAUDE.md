# CLAUDE.md

@AGENTS.md

## Claude Code
- Les règles `.claude/rules/*.md` se chargent automatiquement selon leur frontmatter `paths:` —
  aucun routage manuel.
- Politiques techniques (permissions `deny`, hook `guard-git-push`) : `.claude/settings.json`.
